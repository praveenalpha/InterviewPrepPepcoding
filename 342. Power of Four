class Solution {
public:
    bool isPowerOfFour(int n) {
        long int power = 1, i=0;
        while(power <= n){
            power = pow(4, i);
            if(power == n)  return true;
            i++;
        }
        return false;
    }
};
